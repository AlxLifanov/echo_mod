entity echo.Server

components {
    cserver : echo.CPing // слева экземпляр компонента, справа его класс
}

// В определении entity не запрещено сразу указать endpoints{} (cv CPing.cdl), если нет переиспользования и разработчик ленив.
// При этом название автосгенерённых функций будет немного другим, см secure_logger, echo_mod_dyn для референса.
