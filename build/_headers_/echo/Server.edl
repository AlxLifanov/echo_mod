entity echo.Server

components {
    cserver : echo.CPing // класс компонента определяет вложенные интерфейсы, должен начинаться с большой буквы
}

// В определении entity не запрещено сразу указать endpoints{} (cv CPing.cdl), если нет переиспользования и разработчик ленив.
// При этом название автосгенерённых функций будет немного другим, см secure_logger для референса.
